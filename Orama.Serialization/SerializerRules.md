# Serializer Rules
Each Serializer format follows these rules and NO OTHERS:

	-	Serialize Public Properties by default
	-	Serialize Fields/Private Properties with the [Serialize] Attribute
	-	Never serialize something with the [DontSerialize] Attribute